---
layout: layout
---

<main id="blog" class="page">
	<div class="grid">

		{# {%dynamic_sidebar('top-sidebar');%} #}

		<section id="content" class="blog post">

			<article id="post">


				<div class="thumb" style="background-image:
					url({% if coverImage %}'{{'images/'+coverImage | url}}'{% else %}'{{'/images/default.png' | url}}'{% endif %});">


					<header>
						<h1>{{title}}</h1>
						<div class="meta">
							<span class="date">{{date | date}}</span>
							{% if tags %}
								<span class="divider"></span>
								{% for tag in tags -%}
									<a class="tag" href='{{tag | tagLink}}'>{{tag | i18n}}</a>{% if loop.index !== tags.length %},{% endif %}
								{% endfor %}
							{% endif %}
						</div>
					</header>

				</div>


				<div class="text">
				  {{content | safe}}
{#
					<div class="center">
						<div class='podelis' style="margin: 1rem auto 0;"><img alt="Поделись статьёй с другом!" src='{{'images/Podelis.gif' | url}}' /></div>
						<script type="text/javascript">(function() {
						  if (window.pluso)if (typeof window.pluso.start == "function") return;
						  if (window.ifpluso==undefined) { window.ifpluso = 1;
						    var d = document, s = d.createElement('script'), g = 'getElementsByTagName';
						    s.type = 'text/javascript'; s.charset='UTF-8'; s.async = true;
						    s.src = ('https:' == window.location.protocol ? 'https' : 'http')  + '://share.pluso.ru/pluso-like.js';
						    var h=d[g]('body')[0];
						    h.appendChild(s);
						  }})();</script>
						<div class="pluso" data-background="none;" data-options="big,square,line,horizontal,counter,sepcounter=1,theme=14" data-services="email,vkontakte,odnoklassniki,facebook,twitter,google,livejournal"></div>
					</div> #}

{# 					{% // Generating links to previous and next posts
						$prev_post = get_previous_post();
						$prev_post_url = get_permalink($prev_post->ID);
						$next_post = get_next_post();
						$next_post_url = get_permalink($next_post->ID);
						$current_url = "http://".$_SERVER['HTTP_HOST'].$_SERVER['REQUEST_URI'];
						if( $next_post_url == $current_url )
							$nonext = true;
						elseif( $prev_post_url == $current_url )
							$noprev = true;
					%} #}

{# 					{%if( !$noprev ):%}
						<a class="prev" href="{%=$prev_post_url;%}#post" title="Предыдущая статья: «{%=$prev_post->post_title%}»">&lt; Предыдущая статья<br><span>«{%=$prev_post->post_title%}»</span></a>
					{%endif%}

					{%if( !$nonext ):%}
						<a class="next" href="{%=$next_post_url;%}#post" title="Следующая статья: «{%=$next_post->post_title%}»">Следующая статья &gt;<br><span>«{%=$next_post->post_title%}»</span></a>
					{%endif%}
 #}
				</div>
			</article>

			{# <div id="afterpost" class="sidebar"> #}
				{# {% dynamic_sidebar('endpost-sidebar'); %} #}
			{# </div> #}

			{# <section id="comments"> #}
				{# {% comments_template() %} #}
			{# </section> #}

		</section>

		<aside id="sidebar" class="sidebar">
			{# {% dynamic_sidebar('main-sidebar'); %} #}
			{# {%/*
			<div class="center">
				<a href=/kniga-kak-realizovyvat-mechty/ title='Узнать подробности' alt='Узнать подробности'><img style="margin:25px 0;"  src='/wp-content/uploads/Darim-knigu.png' width=238 height=121 title='Узнать подробности'></a>
			</div>
			*/%} #}
		</aside>

	</div><!-- grid -->
</main>

